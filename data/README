## `data`

This directory contains processed primary data. Text files generated via `image_processing_scripts` were read in, compiled and structered as data frames. 
To ensure anonymization some variables may not be accesible.

R files can be used to run code presented in `statistics`.
