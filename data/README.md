## `data`

This directory contains processed primary data. Text files generated on image processing level were read in, compiled and structered as data frames. 
To ensure pseudonymization some variables may not be accesible.

R files can be used to run code presented in `statistics`.
